<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <% x = task.completed ? "[x]" : "[ ]" %>
    <li>
      <%= x %> <%= link_to task.title, task_path(task) %>
    </li>
  <% end %>
</ul>

<%= link_to "Create Task", tasks_new_path %>



